ex_characters = {'a': 2, 'b': 4, 'c': 5}
ex_sequences = ['aacb', 'caab']

def max_subsequence(characters: dict, sequences: list):
    # iterate through the longest of the two strings
    # build up a set of subsequences that exist in both and their cumulative value
    # return the one of the greatest value

    return 0

if __name__ == '__main__':
    maximum = max_subsequence(ex_characters, ex_sequences)
    print(maximum)